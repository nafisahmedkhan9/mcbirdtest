const host = "https://inventoryapp009.herokuapp.com/"

const URLS =  {
    LOGIN_API: host + "inventory/login/",
    SIGNUP_API: host + "inventory/signup/",
    CUSTOMER_API: host + "inventory/customer/"
}

export default URLS